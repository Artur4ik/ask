Привет!<br><br>

У нас появились новые вопросы, может быть, ты знаешь на них ответ?<br><br><hr>
  <%@q.each do |i|%>
    <table>
      <tr>
        <td><a href=<%="/users/#{i.user_id}"%>><%=image_tag(User.find(i.user_id).avatar_url, width: 80, height: 80, class: "rounded-circle")%></a></td>
        <td>&nbsp;</td>
        <td><h3>
          <%=link_to "Вопрос №#{i.id}", "/questions/#{i.id}" %> от <%=link_to User.find(i.user_id).name, "/users/#{User.find(i.user_id).id}"%></h3><%=i.body%><p align="left"><%=i.created_at.strftime("%d.%m.%Y, %H:%M")%></p></td>
      </tr>
    </table>
    <hr>
  <%end%>
<br><br>
<i>Письмо создано автоматически. Пожалуйста, не отвечайте на него.</i>
