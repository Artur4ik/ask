<!DOCTYPE html>
<html>
  <head>

    <header class="p-3 bg-dark text-white">
      <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">


          <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
            <li><%=link_to "Главная", questions_path, class: "nav-link px-2 text-white"%></li>
            <%if user_signed_in? %>
            <li><%=link_to "Мои вопросы", "/users/#{current_user.id}", class: "nav-link px-2 text-white"%></li>
            <li><%=link_to "Добавить вопрос", new_question_path, class: "nav-link px-2 text-white"%></li>
            <%end %>
          </ul>

          <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-right mb-md-0">
            <li><%=button_to "ВсеВопросы.рф", questions_path, method: :get, class: "btn btn-warning"%></li>
          </ul>

          <div class="text-end">
            <%if user_signed_in? %>
              <table>
                <tr>
                  <td><%= image_tag("autorisated.png") %></td>
                  <td>Выполнен вход: <%=current_user.email%></td>
                  <td><%=button_to "Выйти", destroy_user_session_path, method: :delete, class: "btn btn-outline-light me-2"%></td>
                </tr>
              </table>
              <%else %>
              <table>
                <tr>
                  <td><%=button_to "Войти", new_user_session_path, method: :get, class: "btn btn-outline-light me-2"%></td>
                  <td><%=button_to "Регистрация", new_user_registration_path, method: :get, class: "btn btn-warning"%></td>
                </tr>
              </table>
            <%end %>

          </div>
        </div>
      </div>
    </header>


    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"%>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="container">
    <%= yield %>
  </div>
  </body>
</html>
