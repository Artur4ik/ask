<%@count = Like.where(target_id: @question.id, target_type: "Q", emoji: "👍").count%>

<%=user_signed_in? ? link_to("#{@count} 👍", "javascript:void(0)", id: "#{@question.id}", class: "like-btn", data: { user_id: current_user.id, target_id: @question.id, target_type: "Q", emoji: "👍", url_path: likes_path, method: :post}) : "#{@count} 👍"%>

<%@count = Like.where(target_id: @question.id, target_type: "Q", emoji: "💩").count%>
<%=user_signed_in? ? link_to("#{@count} 💩", "javascript:void(0)", id: "#{@question.id}", class: "like-btn", data: { user_id: current_user.id, target_id: @question.id, target_type: "Q", emoji: "💩", url_path: likes_path, method: :post}): "#{@count} 💩"%>
